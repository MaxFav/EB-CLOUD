<odoo>
    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_view_invoice']" position="after">
                <button name="action_open_payment_wizard" type="object" string="Payment Wizard"/>
            </xpath>
            <xpath expr="//field[@name='invoice_status']" position="after">
                <field name="reconciled_advance_payment_total" widget="monetary"/>
                <field name="outstanding_balance" string="Outstanding Payment" widget="monetary"/>
                <field name="unreconciled_advance_payment_total" widget="monetary"/>
            </xpath>
        </field>
    </record>
    <record id="purchase_order_tree_inherit" model="ir.ui.view">
        <field name="name">purchase.order.tree.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="after">
                <field name="outstanding_balance" invisible="1"/>
            </xpath>
        </field>
    </record>
    <record id="outstanding_purchase_order_tree" model="ir.ui.view">
        <field name="name">purchase.order.tree</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <tree decoration-bf="message_unread==True" decoration-muted="state=='cancel'" decoration-info="state in ('wait','confirmed')" string="Purchase Order" create="false">
                <field name="message_unread" invisible="1"/>
                <field name="name" string="Reference"/>
                <field name="date_order" />
                <field name="partner_id"/>
                <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                <field name="date_planned" invisible="context.get('quotation_only', False)"/>
                <field name="user_id"/>
                <field name="origin"/>
                <field name="amount_untaxed" sum="Total Untaxed amount" string="Untaxed" widget="monetary"/>
                <field name="amount_total" sum="Total amount" widget="monetary"/>
                <field name="reconciled_advance_payment_total" sum="Total Reconciled Prepayments" widget="monetary"/>
                <field name="outstanding_balance" string="Outstanding" sum="Total outstanding" widget="monetary"/>
                <field name="unreconciled_advance_payment_total" sum="Total Unreconciled Prepayments" widget="monetary"/>
                <button name="action_open_payment_wizard" type="object" string="Payment Wizard"/>
                <field name="currency_id" invisible="1"/>
                <field name="state"/>
                <field name="invoice_status" invisible="1"/>
            </tree>
        </field>
    </record>
</odoo>